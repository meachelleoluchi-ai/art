apiVersion: apps/v1
kind: Deployment
metadata:
  name: books-catalog
spec:
  replicas: 2
  selector:
    matchLabels:
      service: books-catalog
  template:
    metadata:
      labels:
        service: books-catalog
    spec:
      containers:
      - image: ghcr.io/meachelleoluchi-ai/art-app:latest
        name: api 
      imagePullSecrets:
      - name: bookcatalog
