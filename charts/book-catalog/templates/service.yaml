apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-catalog.fullname" . }}
  labels:
    {{- include "book-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "book-catalog.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
